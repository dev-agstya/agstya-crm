/// <reference types="vite/client" />
// Vite's ambient types (import.meta.env, import.meta.glob, ?raw imports). The
// standard scaffold file was missing, so anything reaching for import.meta had
// no types to go on.
